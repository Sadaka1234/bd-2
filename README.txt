Realizamos las siguientes modificaciones:
	-En la tabla CLIENTES cambiamos en nombre del atributo "Contraseña" por "Pass" (la ñ T-T)
	- Agregamos el atributo "Nombre" en la tabla Cines.
(Nos imaginamos a SansanoFilms como Cencosud, el cual posee Jumbo, Easy, etc; por eso decidimos
agregar el atributo)

Para realizar las consultas SQL dentro de php, se require al usuario "root" (deberia venir por defecto)
el cual tiene todos los poderes de Modificacion, consulta, etc.) y no tiene contraseña.

Los datos en las tablas CINES y SALAS hay que agregarlos a traves de SQL (no se puede mediante la aplicacion)



Atte.:
	Javier Largo	201473516-2
	Victor Zavala	201473560-K